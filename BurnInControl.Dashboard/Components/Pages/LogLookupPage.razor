@page "/wafer-logs"

<RadzenTabs>
    <Tabs>
        <RadzenTabsItem Text="Recent Logs" Icon="history" Style="background:#4a764a">
            <RecentLogsView/>
        </RadzenTabsItem>
        <RadzenTabsItem Text="Logs/Status By Station" Icon="dns" Style="background: #0a53be">
            <StationLogLookup/>
        </RadzenTabsItem>
        <RadzenTabsItem Text="Wafer Logs" Icon="pageview" Style="background: #cc7119">
            <WaferLogLookup/>
        </RadzenTabsItem>
    </Tabs>
</RadzenTabs>

@code {
    private string? _waferId=string.Empty;
    private IEnumerable<string>? _waferList = [];
    private bool _loading = false;
    /*private List<List<WaferTestReading>>? _waferTestReadings = null;*/

    protected override async Task OnInitializedAsync() {
        //this._waferList = await this.QtDataService.GetQuickTestList(DateTime.Now-TimeSpan.FromDays(90));
    }

    private async Task FetchHandler() {
        
    }

}