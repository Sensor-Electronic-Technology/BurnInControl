@page "/wafer-logs"
@using BurnInControl.Data.BurnInTests
@using BurnInControl.Infrastructure.Dashboard
@using BurnInControl.Infrastructure.QuickTest
@using BurnInControl.Dashboard.Components.AppComponents.LogViews

<RadzenTabs>
    <Tabs>
        <RadzenTabsItem Text="Test Status By Station">
            <StationLogLookup/>
        </RadzenTabsItem>
        <RadzenTabsItem Text="Wafer Logs">
            <RadzenText Text="Coming Soon...." TextStyle="TextStyle.H4"/>
        </RadzenTabsItem>
        <RadzenTabsItem Text="Test Logs By Station">
            <RadzenText Text="Coming Soon...." TextStyle="TextStyle.H4"/>
        </RadzenTabsItem>
    </Tabs>
</RadzenTabs>

@code {
    private string? _waferId=string.Empty;
    private IEnumerable<string>? _waferList = [];
    private bool _loading = false;
    /*private List<List<WaferTestReading>>? _waferTestReadings = null;*/

    protected override async Task OnInitializedAsync() {
        //this._waferList = await this.QtDataService.GetQuickTestList(DateTime.Now-TimeSpan.FromDays(90));
    }

    private async Task FetchHandler() {
        
    }

}